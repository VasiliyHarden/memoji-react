import React from 'react';
import { useSelector } from 'react-redux';

import './Card.scss';

const Card = ({ id }) => {
  const { isOpen, value, state } = useSelector()

  return (
    
  );
}

export default Card;